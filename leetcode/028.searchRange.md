# 28.在排序数组中找第一个元素和最后一个元素的位置

## 题目描述

给你一个按照非递减顺序排列的整数数组 nums，和一个目标值 target。请你找出给定目标值在数组中的开始位置和结束位置。

如果数组中不存在目标值 target，返回 [-1, -1]。

你必须设计并实现时间复杂度为 O(log n) 的算法解决此问题。

 

**示例 1：**

输入：nums = [5,7,7,8,8,10], target = 8

输出：[3,4]

**示例 2：**

输入：nums = [5,7,7,8,8,10], target = 6

输出：[-1,-1]

**示例 3：**

输入：nums = [], target = 0

输出：[-1,-1]

## 解题思路

找目标值的左边界和右边界，对两个函数分别用二分法的思想。

## 代码示例
```python

def searchRange(self,nums,target):
    res=[]
    def findLeft():
        left,right=0,len(nums)-1
        mid=(left+right)//2
        while left<=right:
            if nums[mid]==target:
                res=mid
                right=mid-1
            elif nums[mid]<terget:
                left=mid+1
            else:
                nums[mid]>taregt:
                right=mid-1
        return res
    def findRight():
            left,right=0,len(nums)-1
            res=-1
            while left<=right:
                mid=(left+right)//2
                if nums[mid]>target:
                    right=mid-1
                elif nums[mid]<target:
                    left=mid+1
                else:
                    res=mid
                    left=mid+1
            return res
    i=findLeft()
    if i==-1:
        return [-1,-1]
    j=findRight()
    return [i,j]
