# 59.最长递增子序列

## 代码示例

```python
def lengthofLIS(nums):
  if not nums:
    return 0
  n=len(nums)
  #dp[i]:0-i的最长递增子序列数
  dp=[1]*n
  for i in range(1,n):
    for j in range(1,i):
      if nums[i]>nums[j]:
        dp[i]=max(dp[i],dp[j]+1)
  return max(dp)
