# 61.零钱兑换

```python
def coinChange(self, coins, amount):
        """
        :type coins: List[int]
        :type amount: int
        :rtype: int
        """
        #完全背包问题
        #一定注意索引问题
        #dp 的 i 是从 1 开始数的（为了处理边界），但 coins 数组的下标是从 0 开始的。
…        for i in range(1,n+1):
            dp[i][0]=0
        for i in range(1,n+1):
            for j in range(1,amount+1):
                if j>=coins[i-1]:#！！！！！！
                    dp[i][j]=min(dp[i-1][j],dp[i][j-coins[i-1]]+1)
                else:#这个硬币超amount金额了
                    dp[i][j]=dp[i-1][j]
                
        return dp[n][amount] if dp[n][amount]!=float('inf') else -1
